version: '2'
services:
  # Selenium chrome
  selenium-chrome:
    image: "selenium/standalone-chrome"

  # Pytest
  pytest:
    build: ./loginapp
    command: ./run_pytest.sh
    volumes:
      - ./loginapp:/app
    expose:
      - "8081"
    depends_on:
      - selenium-chrome
      - worker
